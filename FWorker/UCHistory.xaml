<UserControl x:Class="FWorker.UCHistory"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FWorker"
             mc:Ignorable="d" 
             d:DesignHeight="308" d:DesignWidth="1171">
    <Grid Background="LightGray">
        <Grid.RowDefinitions>
            <RowDefinition Height="50">
            </RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid Grid.Row="1" Grid.Column="0">
            <Image x:Name="workImage"  ></Image>
        </Grid>
        <Grid Grid.Row="0" Grid.Column="1">
            <TextBlock x:Name="workerName" Text=""></TextBlock>
        </Grid>
        <Grid Grid.Row="1" Grid.Column="1">
            <TextBox Background="LightGray" x:Name="comment" Text=""></TextBox>
        </Grid>
    </Grid>
</UserControl>
